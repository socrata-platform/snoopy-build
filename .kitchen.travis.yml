--
driver:
  name: localhost

suites:
  - name: default
    run_list: 
      - snoopy-build::default
      <%= '- snoopy-build::deploy' if ENV['PULL_REQUEST'] == 'false' %>
    attributes:
      snoopy_build:
        package_cloud_user: socrata-platform
        package_cloud_repo: snoopy
